---
language: python
sudo: required
python: "2.7"
before_install:
 - sudo apt-get remove --purge fail2ban
 - sudo apt-get install python-software-properties
 - sudo add-apt-repository 'deb http://be.archive.ubuntu.com/ubuntu/ precise-backports main restricted universe multiverse' -y
 - sudo add-apt-repository 'deb-src http://be.archive.ubuntu.com/ubuntu/ precise-backports main restricted universe multiverse' -y
 - sudo apt-get update -qq
 - sudo apt-get install -qq python-apt python-pycurl
install:
  - pip install ansible==1.8.0
script:
  - ansible-playbook -i tests/inventory tests/test.yml --syntax-check
  - ansible-playbook -i tests/inventory tests/test.yml --connection=local --sudo
  - "ansible-playbook -i tests/inventory tests/test.yml --connection=local --sudo | grep -q 'changed=0.*failed=0' && (echo 'Idempotence test: pass' && exit 0) || (echo 'Idempotence test: fail' && exit 1)"
  
